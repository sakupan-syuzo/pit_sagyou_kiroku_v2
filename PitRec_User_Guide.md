# Pit 作業記録アプリ (PitRec V2) ユーザーガイド

PitRec V2 は、レース中のピット作業（PIT IN / OUT時刻、ドライバー交代、タイヤ交換、給油など）をスマートフォンやタブレットで正確・簡単に記録し、PDFやCSVで出力できるWebアプリケーションです。

---

## 📱 1. まずはじめに（事前準備）

レース当日、記録を始める前にエントリーリスト（参加車両とドライバー情報）とピット割り当てをアプリに読み込ませます。

### データの読み込み手順
1. **「登録」タブ** を開きます。
2. 画面上部の **「記録対象レース」** から、登録したいレース枠（例：レース 1）を選びます。
   - ✏️マークをタップすると「SGT」や「SF」など、わかりやすい名前に変更できます。
3. **「📄 PDFから登録」** モードで、大会公式のPDFファイル（エントリーリストやピット割当表）をアップロードします。
4. 読み込まれた表の上のプルダウンから、各列の意味（Car No、PIT No、ドラA など）を指定します。
   - *※「7 山田 太郎」のように車番と名前がくっついている場合は「Car No + ドラA」を選択すると自動で分離されます。*
5. 最後に **「保存して適用」** を押します。

### 手動での修正・追加
読み込みがうまくいかなかった場合や、欠場などの修正が必要な場合は、**「🛠️ 登録済みデータを管理」** タブに切り替えます。
ここで個別にドライバー名を修正したり、右上の「＋ 追加」ボタンから手動で車両を追加できます。

---

## ⏱️ 2. 作業記録をつける（打刻）

準備ができたら、**「作業記録」** タブで実際の計時を行います。初期状態では2台分のレーン（記録枠）が表示されています。

### 記録の流れ
1. **マシンの選択**
   - PIT No. または Car No. を入力すると、登録タブで読み込んだドライバー情報などが自動で反映されます。
2. **PIT IN**
   - マシンがピットに入った瞬間に **「IN」** ボタンをタップします。
   - タイマーが作動し、ピットの滞在時間がリアルタイムで表示されます。
3. **作業内容の入力**
   - ピット作業中に以下の情報を入力します。
     - **ドライバー交代:** 「交代」ボタンを押し、次に乗るドライバーを選択します。
     - **タイヤ交換:** 「＋」「ー」ボタンで交換した本数（0〜4本）を記録します。
     - **給油:** 行った場合は「給油」ボタンをオンにします。
     - **その他:** ペナルティ消化など、特記事項があればテキストでメモを残せます。
4. **PIT OUT**
   - マシンがピットを出た瞬間に **「OUT」** ボタンをタップします。これで1件の記録が完了です！

> 💡 **連続打刻モード**
> 1台がOUTした直後に、すぐ別の作業が入る場合は「連続モード」のチェックを入れておくと、OUTを押した瞬間にPIT NoやCar Noを引き継いだまま次のIN待ち状態になります。

---

## 📤 3. データの出力と確認

記録したデータは **「出力」** タブで確認・書き出しができます。

### 履歴の修正
画面下部の **「📋 作業履歴」** に、記録したデータが一覧で表示されています。
間違えて打刻してしまった場合は、ここをタップすると後から時刻や作業内容を修正・削除できます。

### PDF / CSV での出力・共有
- **セッション名と担当審判員** を入力します（出力するPDF/CSVのヘッダーに印字されます）。
- **PDF保存 / 共有:**
  - 「保存」を押すと端末にPDFが保存されます。
  - 「共有」を押すと、AirDropやLINEなどで他の人に直接送信できます。
- **CSV保存 / 共有:**
  - PCで集計したい場合はCSV形式で出力します。エクセル等で読み込める形式です。

---

## 🤝 4. 複数人でのデータ引き継ぎ（JSON連携）

作業の途中で別の担当者にタブレットを渡す場合や、複数のレースの登録データを他の端末にコピーしたい場合は **「担当引き継ぎ・バックアップ」** 機能を使います。

### データを送る側
1. 出力タブの **「データを送る」** をタップします。
2. 共有メニューが開くので、AirDropやLINEなどでファイルを送ります。

### データを受け取る側
1. 出力タブの **「データを受け取る」** をタップし、送られてきた `.json` ファイルを選択します。
2. **「データの引き継ぎ設定」モーダル** が開きます。
   - 相手がどのレース枠（例：SGT）のデータを送ってきたかが表示されます。
   - 自分の端末の「どのレース枠」にそのデータを上書き・結合するかをプルダウンで選びます。（名前が同じ場合は自動で選択されます）
   - 「インポート実行」を押すと、作業記録とエントリーデータが自分の端末に安全に合体します。

---

## ❓ よくある質問

* **Q. 長いドライバー名がはみ出してしまう**
  * A. 自動で文字サイズが縮小され、省略されるように調整されています。PDF出力時には「A」「B」などアルファベットでスッキリと印字されます。
* **Q. 同時に3台以上のピットインを記録したい**
  * A. 画面下部の「＋ レーンを追加」ボタンを押すことで、最大5レーンまで記録枠を増やすことができます。
* **Q. 間違えて「PIT IN」を押してしまった**
  * A. INボタンを押した直後に表示される「キャンセル」ボタンを押せば、その記録を破棄してやり直せます。
* **Q. アプリを閉じたらデータは消える？**
  * A. データは端末内に自動保存されています。間違えてブラウザを閉じても、再度開けば前回の状態から再開できます。レースが完全に終わり、次の大会に移る際は「登録」タブの「このレースのデータクリア」を行ってください。
